import React from 'react'
import { Container, Typography } from '@mui/material'

const AdminResults: React.FC = () => (
  <Container>
    <Typography variant="h6">Admin Results (coming soon)</Typography>
  </Container>
)

export default AdminResults

import React from 'react'
import { Container, Typography } from '@mui/material'

const ExamList: React.FC = () => (
  <Container>
    <Typography variant="h6">Available Exams (coming soon)</Typography>
  </Container>
)

export default ExamList
